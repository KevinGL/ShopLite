<aside class="md:w-80 w-1/3 flex-none bg-white text-gray-800 border-r border-gray-300 shadow-md">
    <div class="flex flex-col p-4 space-y-2">
        <h2 class="text-lg font-bold mb-4 border-b border-gray-300 pb-2">Catégories</h2>
        
        {% for cat in allCategories %}
            <a href="{{ url('app_by_cat', {id: cat.id, page: 1}) }}"
               class="block px-3 py-2 rounded-lg hover:bg-gray-200 transition">
                {{ cat.name }}
            </a>
        {% endfor %}
    </div>
</aside>
