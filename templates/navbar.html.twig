<nav style="display: flex">
    <a href="{{ url('app_shop') }}">ShopLite</a>
    <form action="{{ url('app_by_search', {'page': 1}) }}" method="get">
        <input type="text" name="words" />
        <input type="submit"/>
    </form>
    <div id="cart">
        <img src="/images/Cart.png" alt="cart" width="20" />
        <span id="count_cart">0</span>
        <a href="{{ url('view_cart') }}">Voir mon panier</a>
    </div>
    <div>
        <a href="{{ url('app_profile') }}"><img src="/images/Profile.png" alt="profile" width="20" /></a>
    </div>

    {% if "ROLE_ADMIN" in app.user.roles %}
        <div>
            <a href="{{ url('app_products', {'page': 1}) }}">Gérer les produits</a>
        </div>
    {% endif %}
</nav>

<script>
    $.get("/api/cart/get_count", (res) =>
    {
        if(res.success)
        {
            $("#count_cart").text(res.message);
        }
    });
</script>